#ifndef CONFIGURE_H
#define CONFIGURE_H
#include <string>

#cmakedefine CONF_PATH std::string(R"(${CONF_PATH})")

#cmakedefine RSRP_TYPE ${RSRP_TYPE}
#cmakedefine POINT_TYPE ${POINT_TYPE}
#cmakedefine PROB_TYPE ${PROB_TYPE}
#cmakedefine ND_MU ${ND_MU}
#cmakedefine ND_SIGMA ${ND_SIGMA}
#cmakedefine EPSILON ${EPSILON}
#cmakedefine ROOT_DIR std::string(R"(${ROOT_DIR})")

#endif
